FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY packages packages
COPY services services
COPY config config
ENTRYPOINT ["python", "-m", "services.pathway_worker.cli"]
CMD ["run"]
