FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt pyproject.toml ./
RUN pip install --no-cache-dir -r requirements.txt psycopg[binary] pyyaml
COPY . .
ENTRYPOINT ["python", "-m", "services.scanner_worker.cli"]
CMD ["run", "--config", "config/postgres-scanner.example.yaml"]
