#!/usr/bin/env bash
set -euo pipefail
if [[ "${1:-}" == "elastic" ]]; then shift; exec python -m packages.elastic_store.cli "$@"; fi
echo "usage: dataobs elastic {plan|apply|status|rollback}" >&2; exit 2
