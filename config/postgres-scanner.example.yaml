scanner_id: local-postgres-scanner
tenant_id: demo-tenant
environment: demo
integration_id: postgres-demo
source_id: postgres-demo
allow_schemas: [public, analytics]
postgres:
  host: localhost
  port: 5432
  database: dataobs_demo
  username: dataobs_metadata
  password_ref: env://POSTGRES_PASSWORD
  sslmode: prefer
  statement_timeout_ms: 5000
options:
  enable_aggregate_profiling: true
