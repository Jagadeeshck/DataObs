# Optional Kyverno reference

This directory is an optional, disabled-by-default reference for Kyverno 1.12 on the chart's Kubernetes 1.30 target. DataObs does not install or require Kyverno. Apply only after operator review and scope the namespace with the `app.kubernetes.io/part-of=dataobs` label. The native validators remain authoritative for portable certification.
