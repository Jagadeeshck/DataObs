# Deprecated four-tower model

The four-tower model has been replaced by the [six-pillar product model](six-pillar-product-model.md). Legacy API inputs such as `full_stack` and `pipeline` are accepted during migration but new responses emit canonical six-pillar values.
