# Live Data Flow Map

The map renders current, bounded topology using Cytoscape. Shape identifies type; colour plus text identifies health; width represents throughput; dashed/translucent edges identify partial evidence. The accessible table is an equivalent keyboard-readable representation.

The explicit budget is 1,000 visible nodes and 2,500 visible edges. Enterprise-wide views require server collapse and progressive expansion.
