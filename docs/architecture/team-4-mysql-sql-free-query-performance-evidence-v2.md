# MySQL SQL-free Query Performance Evidence v2 (design note)

A future, separately reviewed provider generation may consider bounded aggregate Performance Schema evidence that cannot expose digest text, sample text, SQL text, literals, identities, or business values. v1 does not read Performance Schema and keeps query history unsupported.
