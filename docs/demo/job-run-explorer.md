# Job/run explorer demo

The split Compose files pin Elastic, Airflow and Spark versions. `scripts/seed_job_run_demo.py` emits safe metadata for two tenants with overlapping names. The current environment has not executed the real Airflow/dbt, Spark, Elasticsearch, browser, or approval workflow scenarios; the draft PR must remain blocked on that evidence.
