# BigQuery Warehouse Collector v1 audit

The audited base SHA is `c5b7dff6573b6fd0ce76122e7112b726c35d431d`. Before implementation, `python scripts/release/current_terminal_migration.py` dynamically reported `0025_stream_pathway_reliability_production_closure`; `python scripts/release/check_release_metadata.py`, the conflict-marker guard, and generated-artifact policy passed. The executable released set is `0001` through `0025` in `packages.elastic_store.manifest.migrations()` and its computed checksums were verified by the release check. Migration `0022_aws_data_platform_collector` provides strict generic tenant/environment-scoped provider checkpoints, flattened source observations, and collection-run evidence. No migration was added and no released migration was edited.

The pre-change explicit registry was `aws`, `snowflake`, and `databricks`. Searches found no production BigQuery collector or GCP credential implementation. `integrations/dbt/parse_run_results.py` contains only Team 2-owned BigQuery adapter-to-semantic-convention normalization; database scanners contain test/production `information_schema` SQL for their own engines. Neither is reused. Broader GCP product-matrix text is documentation, not a collector. Reused production patterns are SDK capabilities, canonical resource IDs, partial failures, bounded Collection Manager execution/retry/cancellation, durable generic persistence and OCC checkpointing, the trusted `env:` reference convention, Snowflake fixed-projection SQL, and Databricks lazy dependencies/family isolation.

Official optional libraries are lazy-loaded (`google-cloud-bigquery`, `google-cloud-bigquery-reservation`, `google-auth`, `google-api-core`). Authentication supports per-execution ADC, ADC-compatible WIF config supplied by trusted reference, short-lived service-account impersonation, and disabled-by-default referenced legacy keys. Inline credentials, interactive users, executables in WIF sources, custom scopes/endpoints and API keys are rejected.

Supported families are explicit project identity; permission-filtered datasets; tables/views/external/materialized views and bounded columns; models; routines; regional jobs; minute job timeline; delayed table storage; and optional reservations. Separate family/project/location checkpoint scopes are a provider requirement layered over the existing generic store; runtime persistence remains observations-before-checkpoint. Partition metadata, rows/values, query/view/routine bodies, identities, URIs, IAM, logs, lineage, canonical jobs, profiling, monetary cost, other GCP products, UI and packaging are deferred. Security boundaries are fixed SQL, strict identifiers, an operation allowlist, no global clients, safe exception codes, low-cardinality telemetry, permission-filtered coverage, and redaction-safe flattened evidence. Capability remains `functional_unvalidated` pending retained exact-commit hosted evidence.
