# Contributing to the Console

Use pnpm from the committed lockfile. Generate types from `openapi.json`, then run typecheck, lint, formatting, tests, build and bundle budget checks. Do not hand-maintain duplicate API response schemas; generated schema drift is blocking.
