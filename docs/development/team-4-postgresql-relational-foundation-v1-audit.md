# PostgreSQL relational foundation v1 audit

Audited base SHA: `d006339b3f855b67a63bd4625d42d2f1688a354c`. `python scripts/release/current_terminal_migration.py` dynamically derived `0029_team2_data_intelligence_reconciliation`. The Collection Manager registry contained AWS, Snowflake, Databricks, BigQuery, Azure, Trino and Presto. The Scanner Worker explicitly constructs `PostgresConnector` through `packages/agent_sdk.ConnectorRegistry`; it remains an independent `supported_legacy` runtime.

The audit covered `packages/agent_sdk`, `packages/collectors/sdk`, both workers, `integrations/databases/postgres`, `integrations/sql_engine`, Trino, Presto, generic Elastic provider storage, the vertical slice, and scanner tests. Existing connection/secret resolution, discovery, canonicalisation/fingerprint, freshness and bounded profiling implementations are reusable. The legacy quality assertion and OpenLineage passthrough remain compatibility-only; query history is empty. Provider adaptation, privacy-safe projections, TLS posture, fixed statement ownership, database identity and generic observations required refactoring.

Agent SDK request/result/checkpoint types map to Integration SDK contexts, capabilities, observations, partial failures and checkpoint stores. Legacy checkpoints are connector-local; Team 4 uses tenant/integration/capability-scoped Elasticsearch OCC checkpoints and persists observations before advancement. Relational identity adds trusted environment, stable integration, a hashed instance identity, database, schema and relation. Configuration accepts referenced secrets only; the modern default is `verify-full`, while legacy `prefer` remains unchanged with a migration warning. Aggregate profiling is opt-in and RLS-limited.

`migration_required = false`: migration 0022 already supplies strict generic provider observations, collection runs and provider checkpoints; no PostgreSQL-specific index or mapping is needed. Released migrations are untouched. Compatibility is incremental: stop the legacy schedule before enabling Collection Manager, compare evidence, and retain rollback. Future MySQL/MariaDB, SQL Server and Oracle adapters can implement the foundation's identity, fixed statements, bounded execution, structural fingerprint and safe partial-failure contracts without importing SQL-engine or PostgreSQL semantics.
