# Console configuration

Configure the API origin at the reverse proxy, authentication mode at the DataObs API, and optional Kibana allowlisted base URL server-side. Never inject Elasticsearch credentials, Kibana credentials or long-lived bearer tokens into the bundle.
