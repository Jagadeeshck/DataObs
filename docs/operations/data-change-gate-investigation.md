# Investigating a data change gate

Start with status, risk and confidence independently. Review blocking, warning, and unknown reason codes, then each check's evidence status and references. A structurally downstream asset is potentially affected; it is not proof of breakage. Missing baseline means drift is unknown, missing lineage means dependencies are unknown, no observed contract is not compliance, and absent dbt results are not passing tests.
