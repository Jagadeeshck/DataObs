# Operating global search

The `/search` page and Quick Find expose provider status without endpoints or backend bodies. A retry is performed by changing/resubmitting the in-memory query. Roll back by reverting the search route, Quick Find integration and registry together. Monitor latency buckets, failures, timeouts and result-count buckets only. Known limitation: v1 supports assets, data products and monitors.
