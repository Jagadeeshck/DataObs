# Incident escalation operations

Operators should inspect trigger evidence, policy version, source revisions, triggered time, and current level. Acknowledgement records receipt of the escalation only. Resolve the underlying objective/staleness condition to clear it. Replays are safe; repeated evaluation of an unchanged condition must produce no event. Provider paging and automatic remediation are not supported.
