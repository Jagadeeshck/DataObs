# Kafka least privilege

Grant Cluster `Describe` and `DescribeConfigs`, Topic `Describe` and `DescribeConfigs`, and Group `Describe`. Optional next-unread timestamp inspection needs Topic `Read`, is separately reviewed, and defaults off. The Observer never alters topics, groups, configurations, or offsets.
