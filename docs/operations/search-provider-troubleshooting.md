# Search provider troubleshooting

Confirm permission and runtime capability state first, then tenant/environment headers, server filter support, page limit, stable ordering and cancellation. `unavailable` is not an empty result. A timeout or 429 affects only its provider. Request IDs may be displayed, but never expose URLs, stack traces, index names, tenant IDs or response bodies.
