# Upgrade policy

`upgrade-policy.yaml` is authoritative. DataObs presently has no supported predecessor and no retained successful upgrade certification, so no path is labelled supported. Future N-1 certification is a requirement, not current evidence. Readiness fails closed on absent platform, migration, backup/restore, known-issue, configuration, rollback, or exact-SHA evidence.
