# Credential rotation security policy

Secret bytes remain inside external providers/runtime authentication boundaries and never enter Elasticsearch projections, API output, logs, events, evidence, annotations, or support bundles. Plans contain hashes and redacted references, immutable approval content, bounded overlap/deadlines, verification and rollback checks. Promotion requires successful verification; retirement requires promoted evidence. Local state-machine evidence exists; provider and hosted transition evidence is pending.
