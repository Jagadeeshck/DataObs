# Effective access

The future API must call the same evaluator used for enforcement and explain bounded contributions from identity-provider roles, durable bindings, active grants, denials and tenant/environment intersection. It returns final roles/permissions, safe reason codes, policy version, evaluation time and unavailable evidence. It never returns claims, groups, tokens, headers, secrets, credential references or repository details. The API implementation is pending.
