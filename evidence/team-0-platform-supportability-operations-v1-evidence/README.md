# Team 0 platform supportability operations v1 evidence

This checked-in evidence definition is conservative: operational readiness and release remain **blocked / NO_GO**. The CI workflow generates the complete `team-0-platform-supportability-operations-v1-evidence` artifact against `${GITHUB_SHA}`, rejects a mismatched SHA, verifies required files and verifies every checksum. Hosted production certification is intentionally not represented.
