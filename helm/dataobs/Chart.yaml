apiVersion: v2
name: dataobs
description: DataObs Beta deployment package for external Elasticsearch
type: application
version: 0.2.0
appVersion: "0.2.0"
# Intentionally narrow: this is the sole pending certification target in the
# support matrix, not a claim that every later Kubernetes release is supported.
kubeVersion: ">=1.30.0-0 <1.31.0-0"
