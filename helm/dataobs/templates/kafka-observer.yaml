{{- if .Values.kafkaObserver.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dataobs.fullname" . }}-kafka-observer
  labels: {{- include "dataobs.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.kafkaObserver.replicaCount }}
  revisionHistoryLimit: {{ .Values.global.revisionHistoryLimit }}
  strategy: {type: RollingUpdate, rollingUpdate: {maxUnavailable: 0, maxSurge: 1}}
  selector:
    matchLabels: {{- include "dataobs.selector" (dict "Release" .Release "Chart" .Chart "Values" .Values "component" "kafka-observer") | nindent 6 }}
  template:
    metadata:
      labels: {{- include "dataobs.selector" (dict "Release" .Release "Chart" .Chart "Values" .Values "component" "kafka-observer") | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/shared-config.yaml") . | sha256sum }}
{{ toYaml .Values.kafkaObserver.podAnnotations | indent 8 }}
    spec:
      serviceAccountName: {{ include "dataobs.fullname" . }}-kafka-observer
      automountServiceAccountToken: false
      securityContext: {{ toYaml .Values.global.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.kafkaObserver.terminationGracePeriodSeconds }}
      imagePullSecrets: {{ toYaml .Values.global.imagePullSecrets | nindent 8 }}
      containers:
        - name: kafka-observer
          image: {{ include "dataobs.image" (dict "root" . "image" .Values.kafkaObserver.image) | quote }}
          imagePullPolicy: {{ .Values.kafkaObserver.image.pullPolicy }}
          command: {{ toJson .Values.kafkaObserver.command }}
          env:
{{ include "dataobs.commonEnv" . | indent 12 }}
{{ toYaml .Values.kafkaObserver.env | indent 12 }}
          envFrom: {{ toYaml .Values.kafkaObserver.envFrom | nindent 12 }}
          securityContext: {{ toYaml .Values.global.securityContext | nindent 12 }}
          resources: {{ toYaml .Values.kafkaObserver.resources | nindent 12 }}
      nodeSelector: {{ toYaml (default .Values.global.nodeSelector .Values.kafkaObserver.nodeSelector) | nindent 8 }}
      affinity: {{ toYaml (default .Values.global.affinity .Values.kafkaObserver.affinity) | nindent 8 }}
      tolerations: {{ toYaml (default .Values.global.tolerations .Values.kafkaObserver.tolerations) | nindent 8 }}
      topologySpreadConstraints: {{ toYaml (default .Values.global.topologySpreadConstraints .Values.kafkaObserver.topologySpreadConstraints) | nindent 8 }}
{{- end }}
