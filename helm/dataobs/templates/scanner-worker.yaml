{{- if .Values.scannerWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dataobs.fullname" . }}-scanner-worker
  labels: {{- include "dataobs.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.scannerWorker.replicaCount }}
  revisionHistoryLimit: {{ .Values.global.revisionHistoryLimit }}
  strategy: {type: RollingUpdate, rollingUpdate: {maxUnavailable: 0, maxSurge: 1}}
  selector:
    matchLabels: {{- include "dataobs.selector" (dict "Release" .Release "Chart" .Chart "Values" .Values "component" "scanner-worker") | nindent 6 }}
  template:
    metadata:
      labels: {{- include "dataobs.selector" (dict "Release" .Release "Chart" .Chart "Values" .Values "component" "scanner-worker") | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/shared-config.yaml") . | sha256sum }}
{{ toYaml .Values.scannerWorker.podAnnotations | indent 8 }}
    spec:
      serviceAccountName: {{ include "dataobs.fullname" . }}-scanner-worker
      automountServiceAccountToken: false
      securityContext: {{ toYaml .Values.global.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.scannerWorker.terminationGracePeriodSeconds }}
      imagePullSecrets: {{ toYaml .Values.global.imagePullSecrets | nindent 8 }}
      containers:
        - name: scanner-worker
          image: {{ include "dataobs.image" (dict "root" . "image" .Values.scannerWorker.image) | quote }}
          imagePullPolicy: {{ .Values.scannerWorker.image.pullPolicy }}
          command: {{ toJson .Values.scannerWorker.command }}
          env:
{{ include "dataobs.commonEnv" . | indent 12 }}
{{ toYaml .Values.scannerWorker.env | indent 12 }}
          envFrom: {{ toYaml .Values.scannerWorker.envFrom | nindent 12 }}
          securityContext: {{ toYaml .Values.global.securityContext | nindent 12 }}
          resources: {{ toYaml .Values.scannerWorker.resources | nindent 12 }}
      nodeSelector: {{ toYaml (default .Values.global.nodeSelector .Values.scannerWorker.nodeSelector) | nindent 8 }}
      affinity: {{ toYaml (default .Values.global.affinity .Values.scannerWorker.affinity) | nindent 8 }}
      tolerations: {{ toYaml (default .Values.global.tolerations .Values.scannerWorker.tolerations) | nindent 8 }}
      topologySpreadConstraints: {{ toYaml (default .Values.global.topologySpreadConstraints .Values.scannerWorker.topologySpreadConstraints) | nindent 8 }}
{{- end }}
