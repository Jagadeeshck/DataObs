# Airflow integration

Use `apache-airflow-providers-openlineage`; the REST adapter only enriches safe metadata and submits approved, allowlisted actions. Source credentials remain server-side secret references.
