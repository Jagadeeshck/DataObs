# Streams adapter

Streams support is feature-flagged off by default and limited to visibility and bounded significant-event enrichment. Critical incident detection, persistence, deduplication, recovery and notifications do not depend on technical-preview Streams APIs.
