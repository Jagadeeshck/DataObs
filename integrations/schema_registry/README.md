# Schema Registry adapter

This boundary targets the Confluent-compatible HTTPS API, with host allowlisting, bounded timeouts, safe subject escaping, fingerprints instead of schema bodies in projections, and structural Avro/JSON Schema/Protobuf change rules. Apicurio is an extension boundary, not an implemented adapter.
