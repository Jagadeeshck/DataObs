# DataObs domain model

Stable Pydantic contracts for tenant-aware product entities. IDs are deterministic from normalized natural keys. Endpoint metadata accepts non-secret connection descriptors only; credentials are represented by references.
