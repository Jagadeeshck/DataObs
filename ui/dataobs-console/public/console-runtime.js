// Development default. Deployments replace this file with trusted ConfigMap data.
/* global window */
window.__DATAOBS_CONFIG__ = Object.freeze({
  observability: Object.freeze({ enabled: false }),
});
