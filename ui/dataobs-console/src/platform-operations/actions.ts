/** v1 is deliberately read-first: no lifecycle mutation is exposed. */
export const lifecycleActions = [] as const;
export const OCC_CONFLICT_MESSAGE =
  "This resource changed since the page was loaded.";
